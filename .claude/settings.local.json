{
  "permissions": {
    "allow": [
      "Bash(PYTHONPATH=. pipenv run pytest tests/index_aggregator_test.py::TestIndexAggregatorIntegration::test_main_help -v -s)"
    ],
    "deny": []
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "$CLAUDE_PROJECT_DIR/.claude/remove-trailing-spaces.sh"
          },
          {
            "type": "user-prompt-submit",
            "prompt": "The previous action modified code files. Only if there has been significant changes, then ensure a cost-efficiency-reviewer agent is required to analyze the changes for token efficiency and engineering simplicity. Review the modified code for potential optimizations, overengineering patterns, and refactor the code or content to reduce token consumption while maintaining functionality. Do not ultra compact the code as to maintain readability."
          }
        ]
      }
    ]
  },
  "$schema": "https://json.schemastore.org/claude-code-settings.json"
}